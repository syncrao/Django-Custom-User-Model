# Django-Custom-User-Model
Creating a custom user model in Django provides flexibility beyond the default User model, allowing for the addition of custom fields or the modification of authentication methods
